# Demos

Each folder is one slot: **demo-1**, **demo-2**, …

## Files (same in every folder)

```
demo-N/
  Demo.tsx
  demo.css
```

## Wire up

Add your component in the folder, then register in `registry.ts` with `Demo.tsx` and `demo.css` raw imports.
